module.exports = {
  printWidth: 120, // Максимальная длина строки перед переносом
  tabWidth: 2, // Размер таба (по умолчанию 2)
  useTabs: false, // Использовать пробелы вместо табов
  semi: true, // Ставить точку с запятой в конце строки
  singleQuote: true, // Использовать одинарные кавычки
  htmlWhitespaceSensitivity: "ignore", // Не трогать пробелы в HTML
  trailingComma: "es5", // Запятые в объектах и массивах (по стандарту ES5)
  bracketSameLine: false, // В HTML/JSX переносить `>` на новую строку
};
